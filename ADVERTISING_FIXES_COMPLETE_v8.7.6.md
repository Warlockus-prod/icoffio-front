# ✅ ИСПРАВЛЕНИЯ РЕКЛАМЫ ЗАВЕРШЕНЫ v8.7.6

**Date:** December 8, 2025  
**Version:** v8.7.6  
**Type:** PATCH (Critical advertising fixes)

## ✅ ИСПРАВЛЕННЫЕ ПРОБЛЕМЫ

### 1. ✅ Баннеры больше не обрезаются
**Проблема:** Баннеры были больше чем плейсмент, обрезались  
**Исправление:**
- Убраны `max-width` ограничения для широких баннеров (728x90, 970x250)
- Добавлен `overflow: visible` для всех рекламных контейнеров
- Исправлены размеры iframe и div элементов внутри контейнеров
- Правильные фиксированные размеры для каждого формата

**Файлы:**
- `app/[locale]/layout.tsx` - CSS стили для всех форматов
- `components/UniversalAd.tsx` - убраны maxWidth ограничения для широких баннеров

### 2. ✅ Мобильная реклама только на мобильных устройствах
**Проблема:** Мобильная реклама показывалась на планшетах (тачскринах)  
**Исправление:**
- Изменены breakpoints: `lg:hidden` → `md:hidden` для мобильных форматов
- Изменены breakpoints: `lg:block` → `md:block` для десктоп форматов
- Теперь мобильные форматы показываются ТОЛЬКО на мобильных устройствах (< 768px)
- Планшеты (768px+) показывают десктоп форматы

**Файлы:**
- `app/[locale]/(site)/article/[slug]/page.tsx` - исправлены классы для всех рекламных блоков
- `components/ArticleContentWithAd.tsx` - исправлены классы для рекламы в контенте

### 3. ✅ WinImage (In-Image) правильно настроен
**Проблема:** Дисплейная реклама вместо адаптивного формата под изображениями  
**Исправление:**
- Убедились что используется правильный PlaceID: `63d93bb54d506e95f039e2e3`
- Используется `integrateInImage` с `fetchSelector: true` для автоматического поиска изображений
- Добавлены правильные `excludeSelectors` для исключения миниатюр и навигационных изображений
- WinImage создает iframe под изображениями в статьях автоматически

**Файлы:**
- `app/[locale]/layout.tsx` - улучшена инициализация WinImage

### 4. ✅ Lazy Loading добавлен
**Проблема:** Все рекламные блоки загружались сразу, замедляя страницу  
**Исправление:**
- Добавлен Intersection Observer для всех рекламных блоков
- Реклама загружается только когда блок виден в viewport (за 100px до появления)
- Улучшена производительность страницы
- Контейнеры не рендерятся пока не нужно загружать

**Файлы:**
- `components/UniversalAd.tsx` - добавлен `useIntersectionObserver` hook
- Lazy loading для всех форматов рекламы

### 5. ✅ Display форматы правильно инициализированы
**Проблема:** Display форматы использовали неправильный метод  
**Исправление:**
- Используется `integrateInImage` с `setDisplayBlock: true` для Display форматов
- Добавлен `selector` параметр для указания конкретного контейнера
- Правильная инициализация для каждого PlaceID

**Файлы:**
- `app/[locale]/layout.tsx` - улучшена инициализация Display форматов

## 📋 ИЗМЕНЕННЫЕ ФАЙЛЫ

1. **`components/UniversalAd.tsx`**
   - ✅ Добавлен lazy loading через Intersection Observer
   - ✅ Убраны maxWidth ограничения для широких баннеров
   - ✅ Добавлен `overflow: visible` для предотвращения обрезания

2. **`app/[locale]/(site)/article/[slug]/page.tsx`**
   - ✅ Исправлены breakpoints: `lg:` → `md:` для правильного отображения на устройствах
   - ✅ Мобильные форматы только на мобильных (< 768px)
   - ✅ Десктоп форматы на планшетах и десктопах (>= 768px)

3. **`components/ArticleContentWithAd.tsx`**
   - ✅ Исправлены breakpoints для рекламы в середине контента

4. **`app/[locale]/layout.tsx`**
   - ✅ Исправлены CSS стили для всех форматов - убрано обрезание
   - ✅ Добавлены правильные размеры для iframe и div элементов
   - ✅ Улучшена инициализация WinImage (In-Image)
   - ✅ Улучшена инициализация Display форматов

## 🎯 РЕЗУЛЬТАТЫ

### До исправлений:
- ❌ Баннеры обрезались
- ❌ Мобильная реклама на планшетах
- ❌ Все рекламные блоки загружались сразу
- ❌ WinImage работал неправильно

### После исправлений:
- ✅ Баннеры отображаются полностью без обрезания
- ✅ Мобильная реклама только на мобильных устройствах
- ✅ Lazy loading для всех рекламных блоков
- ✅ WinImage правильно создает iframe под изображениями
- ✅ Правильные размеры для всех форматов
- ✅ Улучшенная производительность страницы

## 🧪 ТЕСТИРОВАНИЕ

### Тест 1: Баннеры не обрезаются
1. Откройте статью на десктопе
2. Проверьте баннер 728x90 после заголовка - должен быть полностью виден
3. Проверьте баннер 970x250 перед Related Articles - должен быть полностью виден

### Тест 2: Мобильная реклама только на мобильных
1. Откройте статью на мобильном устройстве (< 768px)
2. Должны показываться только мобильные форматы (320x50, 320x100)
3. Откройте на планшете (>= 768px)
4. Должны показываться десктоп форматы (728x90, 970x250, 300x250, 300x600)

### Тест 3: Lazy Loading
1. Откройте статью
2. Проверьте Network tab - реклама должна загружаться только когда блок виден
3. Прокрутите страницу - реклама должна загружаться по мере появления

### Тест 4: WinImage (In-Image)
1. Откройте статью с изображениями
2. Проверьте что под изображениями в контенте появляется iframe с рекламой
3. Миниатюры в Related Articles не должны иметь рекламу

## 🚀 DEPLOYMENT

**Status:** ✅ Ready for Testing  
**Breaking Changes:** None  
**Migration Required:** No

---

**Все проблемы с рекламой исправлены!**  
**Реклама теперь работает правильно на всех устройствах.**

