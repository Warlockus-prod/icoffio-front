# Инструкция для разработчиков

Спасибо за участие в проекте **ICoffio**! Эта инструкция поможет вам быстро начать работу.

---

## 🚀 Быстрый старт

### 1. Клонирование репозитория
\`\`\`bash
git clone <URL_РЕПОЗИТОРИЯ>
cd icoffio-front
\`\`\`

### 2. Установка зависимостей

#### Web (Next.js):
\`\`\`bash
cd .
npm install
cp .env.example .env.local  # настроить переменные окружения
npm run dev
\`\`\`
Откройте [http://localhost:3000](http://localhost:3000)

#### Mobile (React Native):
\`\`\`bash
cd icoffioApp
npm install

# iOS (только macOS)
cd ios && pod install && cd ..
npm run ios

# Android
npm run android
\`\`\`

---

## 🌿 Работа с ветками

### Структура
- **\`main\`** — продакшн (не трогаем напрямую)
- **\`develop\`** — основная ветка для разработки
- **\`feature/*\`** — ваши рабочие ветки

### Создание новой ветки
\`\`\`bash
# Убедитесь, что у вас свежий develop
git checkout develop
git pull origin develop

# Создайте ветку для фичи/фикса
git checkout -b feature/your-feature-name
\`\`\`

**Соглашение по именам веток:**
- \`feature/telegram-retry-logic\`
- \`fix/admin-image-upload\`
- \`refactor/analytics-queries\`

---

## 💻 Разработка

### 1. Изменения в коде
Вносите изменения только в **файлы вашего модуля** (см. \`MODULE_MANAGEMENT.md\`).

### 2. Коммиты
Используйте понятные сообщения:

\`\`\`bash
git add .
git commit -m "feat(telegram): add retry mechanism for failed jobs"
\`\`\`

**Формат:** \`<type>(module): описание\`

**Типы:**
- \`feat\` — новая функция
- \`fix\` — исправление бага
- \`refactor\` — рефакторинг
- \`docs\` — документация
- \`test\` — тесты
- \`chore\` — обновление зависимостей, конфигов

**Модули:**
- \`admin\`, \`parser\`, \`telegram\`, \`ads\`, \`home\`, \`analytics\`, \`mobile\`

### 3. Push
\`\`\`bash
git push -u origin feature/your-feature-name
\`\`\`

---

## 📬 Pull Request (PR)

### Создание PR
1. Зайдите на GitHub в репозиторий
2. Нажмите **"New Pull Request"**
3. Выберите:
   - **Base:** \`develop\`
   - **Compare:** \`feature/your-feature-name\`
4. Заполните описание:

\`\`\`markdown
## Описание
Добавил механизм повтора для неудачных Telegram-джобов.

## Изменения
- Добавлена функция \`retryFailedJob()\` в \`telegram-database-service.ts\`
- Обновлён UI в \`TelegramDashboard.tsx\` (кнопка "Retry")
- Написан тест \`retryFailedJob.test.ts\`

## Тестирование
- [x] Локальные тесты пройдены
- [x] Проверил в браузере
- [x] Проверил на мобильном (если применимо)

## Скриншоты (если UI)
[приложить скриншот]

## Связанные Issue
Closes #42
\`\`\`

5. Добавьте **лейблы**: \`module:telegram\`, \`enhancement\`
6. **Assign** себя или ответственного
7. Нажмите **"Create Pull Request"**

### Ревью
- Менеджер/коллеги оставят комментарии
- Внесите правки, если нужно
- После одобрения → PR будет смержен в \`develop\`

---

## 🧪 Тестирование

### Перед созданием PR:

#### Web (Next.js):
\`\`\`bash
cd .
npm run build   # проверьте, что билд проходит
npm run start   # запустите prod-версию
\`\`\`

#### Mobile (React Native):
\`\`\`bash
cd icoffioApp
npm test
# Запустите на реальном девайсе/эмуляторе
\`\`\`

### Линтинг
\`\`\`bash
npm run lint
\`\`\`

Если есть ошибки — исправьте перед коммитом.

---

## 📂 Структура файлов

\`\`\`
icoffio-front/
├── app/
│   ├── [locale]/
│   │   ├── admin/       # Админка
│   │   └── (site)/      # Публичные страницы
│   └── api/             # API-роуты
├── components/
│   ├── admin/           # Админские компоненты
│   └── *.tsx            # Общие компоненты
├── lib/                 # Утилиты, сервисы
├── scripts/             # Утилиты для разработки
├── supabase/            # Миграции БД
│
└── icoffioApp/          # React Native Mobile
    ├── src/
    │   ├── screens/
    │   ├── components/
    │   └── services/
    ├── ios/
    └── android/
\`\`\`

---

## 🐛 Отладка

### Web (Next.js)
\`\`\`bash
cd .
npm run dev
# Откройте DevTools в браузере (F12)
\`\`\`

**Логи API:**
- Проверьте терминал, где запущен \`npm run dev\`
- Supabase Studio: [https://supabase.com/dashboard](https://supabase.com/dashboard)

### Mobile (React Native)
\`\`\`bash
# Откройте инспектор
# iOS: Cmd+D → Debug
# Android: Cmd+M → Debug
\`\`\`

**Логи:**
\`\`\`bash
# iOS
npx react-native log-ios

# Android
npx react-native log-android
\`\`\`

---

## 🔐 Переменные окружения

### Web (.env.local)
\`\`\`env
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-key
OPENAI_API_KEY=sk-...
VERCEL_BLOB_READ_WRITE_TOKEN=vercel_blob_...
\`\`\`

**⚠️ Никогда не коммитьте .env файлы!**

Попросите у менеджера доступ к Supabase и другим сервисам.

---

## 📝 Полезные ссылки

- **Документация проекта:** \`PROJECT_MASTER_DOCUMENTATION.md\`
- **Управление модулями:** \`MODULE_MANAGEMENT.md\`
- **Supabase:** [supabase.com/dashboard](https://supabase.com/dashboard)
- **Vercel (деплой):** [vercel.com/dashboard](https://vercel.com/dashboard)
- **Next.js Docs:** [nextjs.org/docs](https://nextjs.org/docs)
- **React Native Docs:** [reactnative.dev](https://reactnative.dev)

---

## ❓ Частые вопросы

### Как запустить скрипт очистки статей?
\`\`\`bash
cd .
node scripts/clean-wordpress-articles.js --dry-run
\`\`\`

### Как сбросить Telegram webhook?
Читайте: \`scripts/README_TELEGRAM_RESET.md\`

### Как создать миграцию БД?
\`\`\`sql
-- Создайте файл в supabase/migrations/YYYYMMDD_название.sql
-- Пример:
CREATE TABLE IF NOT EXISTS test_table (
  id bigint PRIMARY KEY generated always as identity,
  name text NOT NULL
);
\`\`\`

Применение:
- Для Supabase: через Dashboard → SQL Editor
- Локально: через Supabase CLI (если настроено)

---

## 🎉 Спасибо!

Ваш вклад делает проект лучше. Если есть вопросы — не стесняйтесь спрашивать в чате команды или создавать issue на GitHub.
