# 🧹 АВТОМАТИЧЕСКАЯ ОЧИСТКА ПРОБЛЕМНЫХ СТАТЕЙ

## 📋 Описание

Автоматизированный скрипт для поиска и удаления проблемных статей из базы данных:

- 🇷🇺 **Русские статьи** - статьи с русским контентом, заголовками или slug
- 🧪 **Тестовые статьи** - статьи с тестовыми названиями (test, тест, etc)
- ❌ **Статьи с ошибками** - статьи с ошибками извлечения контента
- 📏 **Неполные статьи** - слишком короткие статьи (< 200 символов)
- 🔗 **Подозрительные slug** - односимвольные или слишком короткие slug
- 📄 **Пустые статьи** - статьи без контента

## 🚀 Использование

### Предпросмотр (без удаления)

```bash
npm run clean-problematic:dry
# или
node scripts/clean-problematic-articles.js --dry-run
```

### Реальное удаление

```bash
npm run clean-problematic -- --confirm
# или
node scripts/clean-problematic-articles.js --confirm
```

## 🔍 Что проверяет скрипт

### 1. Русские статьи
- Кириллица в заголовке, контенте или slug
- Паттерны: "русск", "статья", "прорыв", "революционн", "алгоритм", "обучени", "машин"

### 2. Тестовые статьи
- Паттерны: `test`, `тест`, `test article`, `тестовая`, `test post`
- Односимвольные slug: `en`, `ru`, `pl`
- Тестовые slug: `wylsa-com`, `techcrunch-com-ru`, `ai-edited-test`, `ai-2025-ru`, `ai-ru`

### 3. Ошибки извлечения
- "Не удалось автоматически извлечь контент"
- "Failed to automatically extract content"
- "HTTP 403", "Forbidden"
- "Article from wylsa.com"

### 4. Неполные статьи
- Контент короче 200 символов

### 5. Подозрительные slug
- Slug длиной ≤ 2 символов

### 6. Пустые статьи
- Нет контента ни на английском, ни на польском

## 📊 Отчет

Скрипт выводит:

1. **Статистику по причинам** - сколько статей найдено по каждой причине
2. **Список проблемных статей** - ID, заголовок, slug, причины, дата создания
3. **Отчет об удалении** - сколько удалено из Supabase и WordPress

## ⚙️ Конфигурация

### Переменные окружения

```bash
# Supabase (обязательно)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# WordPress (опционально, для удаления из WordPress)
WORDPRESS_API_URL=https://icoffio.com
WP_USERNAME=your_username
WP_APP_PASSWORD=your_app_password
```

## 🔒 Безопасность

- Скрипт требует явного подтверждения (`--confirm`) для реального удаления
- По умолчанию работает в режиме предпросмотра
- Удаляет статьи из обоих источников: Supabase и WordPress (если настроено)

## 📝 Пример вывода

```
🧹 АВТОМАТИЧЕСКАЯ ОЧИСТКА ПРОБЛЕМНЫХ СТАТЕЙ

Режим: 🔍 ПРЕДПРОСМОТР (используйте --confirm для удаления)

🔍 Ищем проблемные статьи в Supabase...

📊 Найдено статей: 150

📊 СТАТИСТИКА ПРОБЛЕМНЫХ СТАТЕЙ:

  🇷🇺 Русский заголовок: 8 статей
  🇷🇺 Русский контент: 12 статей
  🧪 Тестовая статья: 6 статей
  ❌ Ошибка извлечения контента: 3 статьи
  📏 Слишком короткая: 2 статьи

🚨 НАЙДЕНО ПРОБЛЕМНЫХ СТАТЕЙ: 17

1. ID: 123 | "Прорыв в AI: Революционные алгоритмы..."
   Slug EN: N/A | PL: N/A
   Причины: 🇷🇺 Русский заголовок, 🇷🇺 Русский контент
   Создано: 15.01.2025

...

⚠️  Это был предпросмотр. Для реального удаления запустите:
   npm run clean-problematic -- --confirm
```

## 🛠️ API Endpoint

Также доступен API endpoint для программного удаления:

```typescript
POST /api/admin/delete-supabase-article
{
  "articleId": 123,
  // или
  "slug": "article-slug-en"
}
```

## ⚠️ Важные замечания

1. **Резервное копирование**: Перед массовым удалением рекомендуется сделать backup базы данных
2. **Тестирование**: Всегда сначала запускайте с `--dry-run` для проверки
3. **WordPress**: Удаление из WordPress работает только если настроены credentials
4. **Время**: Изменения появляются на сайте в течение 1-2 минут

## 🔄 Интеграция с другими скриптами

Скрипт можно использовать в комбинации с другими инструментами очистки:

```bash
# Очистка проблемных статей
npm run clean-problematic -- --confirm

# Очистка ошибочных статей (старый скрипт)
npm run clean-errors -- --confirm
```

## 📞 Поддержка

При возникновении проблем:
1. Проверьте переменные окружения
2. Убедитесь, что Supabase доступен
3. Проверьте логи скрипта
4. Запустите с `--dry-run` для диагностики

