# 📱 ICoffio Mobile App

Нативное React Native приложение для iOS с интегрированной рекламной системой, отображающее контент с сайта app.icoffio.com.

## 🎯 Особенности

- **📄 Нативный интерфейс** - Полноценное React Native приложение с нативными экранами
- **🔄 Real-time контент** - Автоматическое обновление статей с сайта icoffio.com
- **📱 Мобильная реклама** - Адаптированные VOX рекламные блоки для мобильных устройств
- **🔍 Поиск и категории** - Удобная навигация по контенту
- **🌐 Гибридный подход** - Сочетание нативных экранов и WebView для дополнительных страниц

## 🏗️ Архитектура приложения

### Экраны:
- **HomeScreen** - Главная с лентой статей + рекламные блоки
- **ArticleScreen** - Детальный просмотр статьи с WebView контентом
- **CategoriesScreen** - Список категорий с переходом в WebView
- **SearchScreen** - Поиск статей через API
- **WebViewScreen** - Универсальный браузер для веб-страниц

### Рекламная система:
- **TopBannerAd** (320x50) - Верхний баннер на главной
- **ContentAd** (300x250) - Между статьями и в контенте
- **BottomBannerAd** (320x100) - Нижний баннер
- **InterstitialAd** (full width) - Полноэкранная реклама

## 🚀 Установка и запуск

### Предварительные требования:
```bash
# Необходимые инструменты:
- Node.js 18+
- React Native CLI
- Xcode 14+ (для iOS)
- CocoaPods
- iOS Simulator или физическое устройство
```

### Установка зависимостей:
```bash
# Клонирование проекта
cd /Users/Andrey/App/icoffio-front/icoffioApp

# Установка npm пакетов
npm install

# Установка iOS зависимостей
cd ios
pod install
cd ..
```

### Запуск в разработке:
```bash
# Запуск Metro bundler
npm start

# Запуск iOS приложения
npm run ios

# Альтернативно через React Native CLI
npx react-native run-ios
```

### Запуск на конкретном устройстве:
```bash
# iPhone 15 Pro Max
npx react-native run-ios --simulator="iPhone 15 Pro Max"

# iPad Air
npx react-native run-ios --simulator="iPad Air (5th generation)"

# Реальное устройство
npx react-native run-ios --device
```

## 📁 Структура проекта

```
icoffioApp/
├── src/
│   ├── components/
│   │   └── MobileAd.tsx          # Мобильные рекламные компоненты
│   ├── screens/
│   │   ├── HomeScreen.tsx        # Главный экран со статьями
│   │   ├── ArticleScreen.tsx     # Детальный просмотр статьи
│   │   ├── CategoriesScreen.tsx  # Экран категорий
│   │   ├── SearchScreen.tsx      # Поиск статей
│   │   └── WebViewScreen.tsx     # Универсальный WebView
│   ├── services/
│   │   └── api.ts               # API интеграция с icoffio.com
│   ├── types/
│   │   └── index.ts             # TypeScript типы
│   └── utils/                   # Вспомогательные функции
├── ios/                         # iOS нативные файлы
├── App.tsx                      # Корневой компонент с навигацией
└── package.json                 # Зависимости проекта
```

## 🔧 Конфигурация

### API Endpoints:
```typescript
// src/services/api.ts
const API_BASE_URL = 'https://app.icoffio.com/api';
const WORDPRESS_API_URL = 'https://app.icoffio.com/api/wordpress-articles';
```

### Рекламные Place ID (VOX):
```typescript
// src/components/MobileAd.tsx
const VOX_PLACE_IDS = {
  'mobile-banner': '63da9b577bc72f39bc3bfc68',
  'medium-rectangle': '63da9e2a4d506e16acfd2a36', 
  'large-mobile-banner': '63daa3c24d506e16acfd2a38',
  'interstitial': '63daa2ea7bc72f39bc3bfc72'
};
```

## 🛠️ Разработка

### Добавление новых экранов:
```typescript
// 1. Создайте компонент экрана в src/screens/
// 2. Добавьте тип в App.tsx
export type RootStackParamList = {
  // ... существующие экраны
  NewScreen: {param1: string};
};

// 3. Добавьте в навигатор
<Stack.Screen name="NewScreen" component={NewScreenComponent} />
```

### Добавление рекламных блоков:
```tsx
import {ContentAd} from '../components/MobileAd';

// В любом экране:
<ContentAd style={{marginVertical: 20}} />
```

### Изменение API:
```typescript
// Добавьте новые методы в src/services/api.ts
class ApiService {
  async getNewData(): Promise<NewDataType> {
    // Ваша логика
  }
}
```

## 📱 Тестирование

### На симуляторе:
```bash
# Различные устройства
npx react-native run-ios --simulator="iPhone 15"
npx react-native run-ios --simulator="iPhone 15 Plus" 
npx react-native run-ios --simulator="iPad Pro (12.9-inch) (6th generation)"
```

### Проверка функций:
- ✅ Загрузка списка статей
- ✅ Переход к детальной статье  
- ✅ Отображение рекламных блоков
- ✅ Поиск по статьям
- ✅ Навигация по категориям
- ✅ Sharing статей
- ✅ WebView для внешних ссылок

## 🚀 Сборка для production

### Debug сборка:
```bash
npx react-native run-ios --configuration Release
```

### App Store сборка:
```bash
# 1. Откройте Xcode
open ios/icoffioApp.xcworkspace

# 2. В Xcode: Product → Archive
# 3. Следуйте инструкциям в APP_STORE_PUBLICATION_GUIDE.md
```

## 📊 Мониторинг и аналитика

### Логирование:
```typescript
// Просмотр логов в development
console.log('Debug info:', data);

// В production используйте React Native Flipper или Firebase
```

### Рекламная аналитика:
- VOX Display предоставляет собственную аналитику
- Интеграция через их dashboard
- Отслеживание CTR и доходности

## 🔄 Обновления

### Контент:
- Статьи обновляются автоматически через API
- Нет необходимости обновлять приложение для нового контента
- Pull-to-refresh для мануального обновления

### Приложение:
```bash
# Увеличьте версию в package.json
"version": "1.0.1"

# Обновите iOS версию в Info.plist
CFBundleShortVersionString: 1.0.1
CFBundleVersion: 2
```

## 📞 Поддержка и вопросы

### Полезные команды:
```bash
# Очистка кеша Metro
npx react-native start --reset-cache

# Переустановка iOS pods
cd ios && rm -rf Pods && pod install && cd ..

# Очистка и пересборка
rm -rf node_modules && npm install
```

### Частые проблемы:
1. **Metro bundler не запускается** → `npx react-native start --reset-cache`
2. **iOS сборка падает** → Переустановите pods: `cd ios && pod install`
3. **WebView не загружается** → Проверьте сетевое соединение и URL
4. **Реклама не показывается** → Убедитесь что VOX скрипт загружается

## 📚 Документация

- [React Native Docs](https://reactnative.dev/docs/getting-started)
- [React Navigation](https://reactnavigation.org/docs/getting-started)
- [React Native WebView](https://github.com/react-native-webview/react-native-webview)
- [App Store Publication Guide](./APP_STORE_PUBLICATION_GUIDE.md)

---

**Happy coding! 🎉**